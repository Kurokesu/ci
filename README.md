# Selftest fixture: recipe

Minimal DKMS packaging recipe for `selftest.yml` on the `main` branch. Pairs with the `selftest/upstream` branch the way a caller's `debian/latest` pairs with its `main`. Not for installation.
