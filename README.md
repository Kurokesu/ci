# Selftest fixture: recipe, pre-release

Minimal DKMS packaging recipe for `selftest.yml` on the `main` branch, carrying a semver pre-release version in Debian form. Pairs with the `selftest/upstream-pre` branch the way a caller's `debian/latest` pairs with its `main`. Not for installation.
